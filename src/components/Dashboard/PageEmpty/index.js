import { Container } from "./style";

export default function PageEmpty({name}){
    return(
        <Container>
            <h1>Você não tem registros nesta página!</h1>
        </Container>
    );
}